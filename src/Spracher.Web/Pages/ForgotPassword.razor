@page "/forgot-password"
@inject AuthApiClient AuthApiClient

<PageTitle>Odzyskiwanie hasła — Spracher</PageTitle>

<section class="form-page">
    <p class="eyebrow">Bezpieczeństwo konta</p>
    <h1>Odzyskaj hasło</h1>
    <p class="form-intro">Jeśli konto istnieje, wyślemy link do ustawienia nowego hasła.</p>

    @if (_sent)
    {
        <div class="alert alert-success" role="status">Sprawdź swoją skrzynkę e-mail.</div>
    }
    else
    {
        <ApiErrorSummary ErrorMessage="@_errorMessage" Errors="@_errors" />
        <EditForm Model="@_model" OnValidSubmit="SubmitAsync" FormName="forgot-password" class="form-card">
            <DataAnnotationsValidator />
            <div class="field">
                <label for="forgot-email">E-mail</label>
                <InputText id="forgot-email" type="email" autocomplete="email" @bind-Value="_model.Email" />
                <ValidationMessage For="@(() => _model.Email)" />
            </div>
            <button class="primary-button" type="submit" disabled="@_isSubmitting">Wyślij link</button>
        </EditForm>
    }
</section>

@code {
    private readonly EmailModel _model = new();
    private IReadOnlyDictionary<string, string[]> _errors = new Dictionary<string, string[]>();
    private string? _errorMessage;
    private bool _isSubmitting;
    private bool _sent;

    private async Task SubmitAsync()
    {
        _isSubmitting = true;
        try
        {
            var result = await AuthApiClient.ForgotPasswordAsync(
                new ForgotPasswordRequest(_model.Email));
            _sent = result.Succeeded;
            _errorMessage = result.ErrorMessage;
            _errors = result.Errors;
        }
        catch (HttpRequestException)
        {
            _errorMessage = "Nie udało się połączyć z API.";
        }
        finally
        {
            _isSubmitting = false;
        }
    }

    private sealed class EmailModel
    {
        [Required(ErrorMessage = "Podaj adres e-mail.")]
        [EmailAddress(ErrorMessage = "Podaj poprawny adres e-mail.")]
        public string Email { get; set; } = string.Empty;
    }
}
